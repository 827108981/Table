# 百度地图API配置指南

## 为什么需要百度地图API？

当前系统使用区县级坐标映射来定位医院位置，但对于乡镇级别的医院，坐标不够精确。
百度地图API可以将详细地址转换为精确的经纬度坐标（地理编码功能）。

## 申请步骤

### 1. 注册百度账号
- 访问：https://lbsyun.baidu.com/
- 如果没有百度账号，先注册一个

### 2. 创建应用
1. 登录后，进入"控制台"
2. 点击"创建应用"
3. 应用名称：填写"迈瑞技术支持数据中心"（或其他名称）
4. 应用类型：选择"服务端"
5. 启用服务：勾选"地理编码"
6. IP白名单：填写 `0.0.0.0/0`（允许所有IP访问）
7. 点击"提交"

### 3. 获取API Key
- 创建成功后，在应用列表中复制"访问应用（AK）"
- 格式类似：`xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### 4. 配置到项目中
打开 `getHospital.py` 文件，找到第 154 行：
```python
BAIDU_AK = "你的百度地图AK"  # 替换为你的API Key
```

将 `你的百度地图AK` 替换为你刚复制的AK，例如：
```python
BAIDU_AK = "aBcDeFgHiJkLmNoPqRsTuVwXyZ123456"
```

## API调用限制

- 免费额度：每日 6000 次请求
- 并发限制：每秒 120 次
- 对于本系统，每次数据爬取会调用一次API（每个工单地址一次）
- 如果工单数量较多，建议监控使用量

## 不使用API的情况

如果不想使用百度地图API，系统仍可正常工作：
- 会通过区县级和城市级坐标映射来定位
- 精度稍低，但覆盖全国主要城市
- 将 `USE_GEOCODING = False` 即可关闭API调用

## 测试API是否配置成功

运行 `getHospital.py`：
```bash
python getHospital.py
```

如果看到以下输出，说明API配置成功：
```
✅ 使用百度API解析成功: 广东省深圳市南山区 -> [113.93, 22.53]
```

如果看到以下输出，说明降级到本地映射：
```
📍 匹配到区县: 广东省深圳市南山区 -> 南山
```

## 常见问题

**Q: API调用失败怎么办？**
A: 系统会自动降级到本地映射，不会影响程序运行

**Q: 需要付费吗？**
A: 每日6000次免费调用，超出后才需要付费

**Q: 可以在多台机器使用同一个AK吗？**
A: 可以，但建议在每台机器上分别申请
